version: '3.8'

services:
  nextjs:
    build: .
    env_file: ./config/env/dev.env
    volumes:
      - ./packages/landingpage:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/.next
    ports:
      - 3000:3000
